import React from "react";
import styled from "styled-components";

interface IProps {}

const Container = styled.div``;
const ProfilePhotoBox: React.FC<IProps> = () => {
  return <Container />;
};

export default ProfilePhotoBox;
